package com.yellowneedle.yellowneedle.ui.theme

import androidx.compose.ui.graphics.Color

val offWhite = Color(0xF6F9FD).copy(alpha = 1f)
val offBlack = Color(0xFF000000).copy(alpha = 1f)

val elementBlack = Color(0xFFFFFF).copy(alpha = 1f) //
val elementWhite = Color(0xFFFFFF).copy(alpha = 1f) //

val whiteText = Color(0xE6E6E6).copy(alpha = 1f)
val blackText = Color(0x141414).copy(alpha = 1f)

val grayTextWhite  = Color(0x888A8C).copy(alpha = 1f)
val grayTextBlack  = Color(0x737373).copy(alpha = 1f)

val stroke = Color(0xDBE6FF).copy(alpha = 1f