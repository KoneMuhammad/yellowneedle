package com.yellowneedle.yellowneedle.ui.theme

import androidx.compose.ui.graphics.Color

val offWhite = Color(0xF6F9FD).copy(alpha = 1f)
val offBlack = Color(0xFF000000).copy(alpha = 1f)

val CalmGray = Color(0xFFFFFF).copy(alpha = 1f) //

val white = Color(0x141414).copy(alpha = 1f)
val black = Color(0xF8FBFF).copy(alpha = 1f)
val grey = Color(0x737373).copy(alpha = 1f)

val stroke = Color(0xDBE6FF).copy(alpha = 1f)